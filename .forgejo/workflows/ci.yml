on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    runs-on: docker
    container:
      image: node:22-slim
    steps:
      - uses: actions/checkout@v4
      - name: Validate HTML
        run: |
          npx --yes html-validate "*.html" "**/*.html" || true
