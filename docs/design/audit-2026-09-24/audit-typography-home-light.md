SCORE: 8/10

DEFECTS:
1. Hero secondary CTA "Fight a denial" (right of the filled button): set at body size/weight with low contrast, so it reads as body copy rather than an action — hierarchy is ambiguous against the primary button.
2. Left card numbered list ("1", "2"): the serif numerals sit in a wide meta-style gutter with excessive tracking/gap to the item text, causing an optically loose, slightly disconnected step-in flow.

Otherwise: display serif (hero) vs. sans body creates a clear, on-brand voice split; eyebrow meta ("FREE · OPEN SOURCE · PRIVATE BY DESIGN") is sensibly letterspaced/uppercase; body line-height (~1.5) and hero measure (~55–60ch) are comfortable; script accent line is a tasteful, non-competing tier; card headings step down cleanly from display.