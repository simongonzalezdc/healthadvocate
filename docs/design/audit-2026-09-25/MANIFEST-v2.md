# G005 pre-registered judgment manifest (v2) — round 2 of 3

Round 1 failed (receipts retained at /tmp/ha-vr/g005-verdicts + ledger checkpoint via
steer subgoal). Fix wave: commit 1697b30 (swatch single-rule + gaps, nav 44px + uniform
gap, ES 44px, numeral ink, stamp symmetry, pill parity, friend un-misted/inked + planter,
scene capped). Same shot set, judges, rubrics, thresholds as v1 — NOTHING moved except
the build under test. Judged once, as-is. This is round 2; one more failure → round 3;
a third failure blocks with the honest scorecard.
