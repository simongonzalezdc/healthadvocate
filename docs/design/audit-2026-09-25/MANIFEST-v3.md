# G005 pre-registered judgment manifest (v3) — round 3 of 3 (FINAL)

Rounds 1-2 failed; receipts retained (/tmp/ha-vr/g005-verdicts r1 in ledger, r2 g005-verdicts).
Round-3 build: inner-warmth all views (commits 3e2fafc..) + regression fixes (dots restored,
dup-class, scene collisions, contrast lifts). Same shot set, judges, rubrics, thresholds.
This is the FINAL round: pass → merge+live; fail → BLOCKED with the honest scorecard
(thresholds never move; next move is the CEO's).
