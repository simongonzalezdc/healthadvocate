judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/final/dark-home.png)

Reading the attached dark-home.jpg directly — no files read or written.

1. **ABSENT** — no "This needs a human decision" banner appears anywhere, but no machine answer or fallback appears on this screen either (it's a home/landing page), so there is nothing it would need to outrank.

2. **ABSENT** — no gray/dashed-border/sentence-case "unavailable" state exists; the nearest neutral element is the "NONE" pill on the 24 SEP item, which sits in the exact slot of the urgency badges with identical filled-pill morphology (it could read as a fourth level of an urgency scale, though its gray fill makes a danger misread unlikely).

3. **PRESENT** — filled uppercase pills DUE SOON (amber), UPCOMING (gray), OVERDUE (red) in the "What's coming up" list; distinction from an unavailable state is unverifiable because none exists on this screen, and the gray "NONE" pill shares the same badge shape, weakening the contrast.

4. **ABSENT/PARTIAL** — only one provenance chip is visible ("✉ FROM THE CALL" on the Sep 26 item); no extracted/inferred/unverified/you-confirmed species are shown, so distinguishability is not demonstrated, and the inference cue "detected in the Sep 24 call" is rendered in the same dim metadata style as confirmed facts — an inferred value could pass as confirmed at a glance.

5. **YES** — a "DEMO DATA" pill sits beside the "What's coming up" heading with the caption "Synthetic examples so you can see how reminders look. Real ones are created on this device only."

6. **YES** — OVERDUE pairs a filled red pill with a full red card border (20 SEP row), clearly stronger than DUE SOON's amber pill/dot and the neutral gray UPCOMING.

HONESTY-VERDICT: PASS — no violation, since no fallback/machine answer masquerades as confident on this screen; two advisories to fix: (a) the "NONE" pill (24 SEP row, right edge) uses urgency-badge morphology instead of neutral-state styling, and (b) the inferred marker "detected in the Sep 24 call" (Sep 26 row) lacks a distinct provenance species and could read as confirmed.
