judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/final/light-library.png)

Auditing the attached Library screen only (image is visible; no files read or written).

1. **'This needs a human decision' banner: ABSENT (as a banner).** The only human-decision notice is small print above the list — "Machine-derived items: anything marked INFERRED or UNVERIFIED was computed by the app, not quoted — confirm it before relying on it" — which is visually weaker than the filled orange "COMPUTED FROM THE LETTER" chip it governs.

2. **Neutral 'unavailable' state: ABSENT.** No gray/dashed/sentence-case unavailable element appears anywhere; the only dashed element is the "DEMO DATA" badge, and nothing on screen could be mistaken for an urgency rating or danger because no such state exists.

3. **Urgency badges: ABSENT.** No LOW/MEDIUM/HIGH filled pills; every list chip ("denial — not medically necessary", "$1,200.00", "Dr. M. Patel", etc.) is a neutral outlined pill, so there is no collision risk with an unavailable state (which is also absent).

4. **Provenance chips: NOT DISTINGUISHABLE at chip level.** The four species (extracted/inferred/unverified/you-confirmed) are not marked on any individual chip; however, the single inferred value on screen (the Oct 8 appeal date) cannot pass as confirmed — it carries the orange "COMPUTED FROM THE LETTER" card badge plus in-body hedge "the app computed Oct 8. Confirm the exact date with the insurer." Residual risk: unmarked chips like "$1,200.00" rely entirely on the header-note convention for their provenance.

5. **Demo/synthetic data badged: YES.** "DEMO DATA" pill with dashed border sits at the top-right of the Library card.

6. **Danger stronger than caution: NO.** The danger marker ("Denies", small red text tag, bottom of the Aetna card) is visually weaker than the caution styling (filled orange "COMPUTED FROM THE LETTER" chip, orange "1 reminder due soon" pill); mitigating context: no emergency-grade content is on this screen, and the most safety-critical item (appeal deadline) being the most prominent is honest emphasis rather than inverted salience.

Core product law holds: the one fallback on screen (computed Oct 8 date) is loudly badged, hedged in its own body text, and instructs human confirmation — it does not read as a confident answer. The gaps are hierarchy weaknesses (weak small-print honesty notice, no per-chip provenance, red tag quieter than orange caution), not a fallback masquerading as an answer.

HONESTY-VERDICT: PASS — violations: none fatal; noted weaknesses: (a) human-decision notice is small print weaker than the machine-answer chip it covers (top of list, above Aetna card); (b) per-chip provenance species unmarked across all four cards; (c) red "Denies" danger tag visually weaker than orange caution chip (Aetna card, bottom-right).
