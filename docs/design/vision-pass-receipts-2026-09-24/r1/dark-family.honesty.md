judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r1/dark-family.png)

Judging the attached dark-mode Family Health Tracker screenshot only.

1. **"This needs a human decision" banner: ABSENT.** No banner exists, but there is also no machine answer anywhere on the screen (only an empty-state form), so the law has nothing to violate here.

2. **Neutral unavailable/empty state: PRESENT — and NO, not mistakable for urgency or danger.** "No family members added yet. Add someone above." is muted gray, sentence case, with a plain clock icon and no fill or red styling, so it reads as informational, not as a rating or warning (note: border is solid, not dashed).

3. **Urgency badges: ABSENT.** No LOW/MEDIUM/HIGH pills exist on this screen; the only pill is the green "1 due soon" header chip, which shares no shape/color with any unavailable state, so no confusion is possible.

4. **Provenance chips: ABSENT.** No extracted/inferred/unverified/you-confirmed chips are rendered, and with zero data rows there are no values whose provenance could be disguised.

5. **Demo/synthetic data badging: ABSENT (nothing to badge).** The screen contains only user-entered form fields and an empty state — no synthetic data is shown, and no DEMO/SAMPLE badge appears.

6. **Danger vs. caution styling: ABSENT — both.** There is no red/danger styling and no amber/caution styling anywhere; the palette is dark gray with a single green accent, so no hierarchy violation can be assessed or violated.

**HONESTY-VERDICT: PASS** — No violation. This screen is an empty-state capture: no machine answer, fallback, urgency badge, provenance chip, or synthetic data exists, so the one honesty-relevant element (the neutral "No family members added yet" state, center of the card) behaves honestly and cannot be mistaken for a confident answer, a rating, or a danger.
