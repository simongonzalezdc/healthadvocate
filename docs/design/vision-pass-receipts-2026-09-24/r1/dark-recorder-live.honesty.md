judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r1/dark-recorder-live.png)

I can see the image (dark-mode Call Recorder screen, demo mode active). Audit follows.

1. **Banner: ABSENT** — no "This needs a human decision" banner anywhere on this recorder screen; the only callouts are the outlined "DEMO MODE" pill and the green privacy notice, so there is no machine answer competing with it (strength comparison N/A).

2. **Unavailable state: ABSENT / not mistakable** — no gray or dashed-border "unavailable" element exists; the nearest honesty device is the text-only convention under the transcript ("*Italic* text is still a draft and may change… the recorder never guesses"), and since no such state is rendered, nothing could be mistaken for urgency or danger.

3. **Urgency badges: ABSENT** — no LOW/MEDIUM/HIGH filled pills on screen; the only pill-like elements are the header "2 due soon" (orange outline, a scheduling nudge) and the DEMO MODE badge, so no collision with an unavailable state.

4. **Provenance chips: ABSENT** — no extracted/inferred/unverified/you-confirmed chips; the only provenance cue is the italic-equals-draft convention (visible on the lighter words "six weeks of physical therapy"), so chip species can't be evaluated — an inferred word is styled only as lighter/italic and is explicitly disclosed as changeable, so it doesn't claim confirmation, but the distinction is typographically weak (single visual channel).

5. **Demo badging: YES** — labeled three separate ways: "DEMO MODE" outlined pill at card top-right, "RECORDING DEMO" beside the timer, and "Start over with the synthetic call script" / "New demo recording" at the bottom.

6. **Danger vs caution: ABSENT (nothing to compare)** — no emergency/danger element on this screen; red is reserved for recorder semantics (pulsing dot, filled "Stop & save") and does read stronger than the orange caution "2 due soon" pill, but that's recording state, not a danger banner.

Minor observation (not a violation): the synthetic library entry "Anita — MRI denial call — Sep 24, 11:02" isn't individually re-badged, but it sits entirely inside the DEMO MODE card directly above the "synthetic call script" link, so context covers it.

HONESTY-VERDICT: PASS — no fallback or guess is presented as a confident answer on this screen: uncertainty is explicitly disclosed ("italic text is a draft", "the recorder never guesses"), demo status is labeled repeatedly, and nothing implies machine certainty. The banner, unavailable state, urgency badges, and provenance chips are ABSENT, so their honesty behavior is not evaluable from this screen type.
