judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r1/dark-recorder.png)

I can see the attached image (dark-theme HealthAdvocate "Recorder" screen) and will judge only it.

**Honesty audit — dark-recorder.jpg (Recorder / demo recording screen)**

1. **NO.** No "This needs a human decision" banner appears anywhere; the only emphasis elements are the consent checkbox and green "Start demo recording" button, with no machine answer present to compare against.

2. **ABSENT.** There is no gray/dashed/sentence-case "unavailable" state on this screen, so nothing here can be mistaken for an urgency rating or danger — the only caution-toned element is the header "2 due soon" pill, which reads as a count, not a fallback.

3. **ABSENT.** No LOW/MEDIUM/HIGH filled pills exist; the sole filled badge is the amber "2 due soon" header pill, and with no unavailable state on screen there is nothing to be distinct from.

4. **ABSENT.** No provenance chips (extracted / inferred / unverified / you-confirmed) appear, so the species question is untestable on this screen — no inferred value can pass as confirmed because none is shown.

5. **PRESENT — clearly badged.** Demo/synthetic status is labeled redundantly and unambiguously: "DEMO MODE" pill at top of the card, "Start demo recording" button, "YOUR RECORDINGS (DEMO)" heading, and "Start over with the synthetic call script."

6. **NO.** No danger/emergency styling exists to compare — the dominant accents are green (privacy banner "Stays on this device — never uploaded, never sent" and the primary button), with only the small amber "2 due soon" pill as caution, so nothing overclaims danger but severity hierarchy can't be confirmed here.

Note: this screen contains none of the audited fallback/badge components (no machine answers, no urgency set, no provenance chips), so it cannot violate the product law; the one honesty-relevant system present — demo/synthetic labeling — is done well.

**HONESTY-VERDICT: PASS**
