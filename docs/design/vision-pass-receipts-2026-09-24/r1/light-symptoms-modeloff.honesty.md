judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r1/light-symptoms-modeloff.png)

I can see the attached image and will judge only it — no files read or written.

1. **YES** — "This needs a human decision." leads the response area in bold red on a pink panel with a red left rule, and it is decisively the heaviest element on the page; the machine answers below ("headache (94%)", "dizziness (85%)") are plain gray/black text with no styling.

2. **YES** — "Model unavailable — no urgency assessment was made." appears as a light-gray, sentence-case pill under URGENCY LEVEL (the border is too faint to confirm dashed at this resolution); it cannot be mistaken for a rating or danger — it has no red/amber/green fill and its wording explicitly negates an assessment.

3. **ABSENT** — no LOW/MEDIUM/HIGH filled pills appear anywhere (consistent with model-off); the only chip-like marker is a tiny lowercase "low" tag beside "Name overlap", clearly unlike a filled urgency pill, so no collision is possible in this frame.

4. **ABSENT** — no extracted / inferred / unverified / you-confirmed chips exist anywhere in the frame, so the species are not distinguishable, and "headache (94%)" / "dizziness (85%)" display confidence-like percentages with zero provenance marking — an inferred or unverified value passes as a confident answer.

5. **ABSENT** — no demo/synthetic badge appears, even though the struck-through banner text ("…skipped using stripped validation errors") shows this is a rigged test run.

6. **YES** — the red crisis banner (988 and SAMHSA numbers, bold heading, red rule) is far stronger than everything else; the only competing states are neutral grays and no amber/caution styling exists to dilute it.

**HONESTY-VERDICT: FAIL** — Violation: in the model-off fallback, the POSSIBLE CONDITIONS block (mid-card, ~60% down) still shows "headache (94%)" and "dizziness (85%)" as unmarked, confidence-scored answers with no provenance chips, so the fallback reads as a confident machine diagnosis and contradicts the banner's own claim that "HealthAdvocate would not answer." Secondary: no demo/synthetic badging despite the frame evidently being a synthetic test run.
