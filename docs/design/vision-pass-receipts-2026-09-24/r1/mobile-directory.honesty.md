judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r1/mobile-directory.png)

Judging only the attached image (no files read or written).

1. **ABSENT** — No "This needs a human decision" banner anywhere; the nearest analogs are per-field "Confirm" buttons beside each amber INFERRED chip (e.g., Patel email, Riverside hours, Aetna fax), and since no banner exists the "stronger than machine answer" test is moot (the inferred values themselves are amber/downgraded, not confident-styled).

2. **ABSENT** — No unavailable state appears; every field on all four cards is populated, so nothing could be mistaken for an urgency rating or danger.

3. **ABSENT** — No LOW/MEDIUM/HIGH pills exist; the only filled pill is the header "1 due soon" badge, a due-date nudge in amber, so distinctness from an unavailable state cannot be evaluated.

4. **PRESENT, distinguishable — YES / could inferred pass as confirmed — NO** — Three visible species are color- and label-coded: YOU CONFIRMED in green with a checkmark, INFERRED · PATTERN / VOICEMAIL / LETTER in amber with a glyph, and extracted-source chips (FROM A CALL, CALL, VOICEMAIL, DOCUMENT, BILL DECODE) in blue-gray; every inferred chip carries the literal word "INFERRED" plus a Confirm button, so it cannot pass as confirmed. Minor note: the lone "VOICEMAIL" chip on Riverside's phone (extracted) vs "INFERRED · VOICEMAIL" on its hours (inferred) differ only by the amber color and prefix — distinguishable, but the prefix is doing the work.

5. **YES** — A dashed-border "DEMO DATA" stamp sits on the directory intro card, reinforced by 555-prefix phone numbers and a `.example` email domain.

6. **ABSENT (N/A)** — No danger/emergency styling appears on this screen to compare against caution styling; the only caution accent is amber (the "due soon" pill and INFERRED chips), and no red/danger element is outranked or diluted by it.

Overall: no fallback answer is presented anywhere on this screen, so the "fallback must never look like a confident answer" law is not triggered; all machine-derived values are visibly downgraded (amber, labeled, Confirm CTA), confirmed values are green and verified, and synthetic data is badged.

HONESTY-VERDICT: PASS
