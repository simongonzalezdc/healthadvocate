judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r1/mobile-recorder.png)

Judging only the attached screenshot (Recorder tab, pre-recording state — no transcript, summary, or urgency output is on this screen).

1. **ABSENT** — no "This needs a human decision" banner anywhere; closest is the footer line "HealthAdvocate is here to support, not replace, professional medical guidance," and no machine answer appears near it to compete with.

2. **ABSENT** — no gray/dashed/sentence-case "unavailable" state is shown; nothing on this screen could be mistaken for an urgency rating or danger (the only dashed element is the blue "DEMO MODE" chip, which reads as a mode badge, not an error).

3. **ABSENT** — no LOW/MEDIUM/HIGH filled pills exist; the only alert-like element is the outlined tan "● 1 due soon" pill in the header, which is a reminder badge, not an urgency rating, and it is visually distinct from everything else.

4. **ABSENT** — no provenance chips (extracted / inferred / unverified / you-confirmed) appear, so no species-mixing or inferred-passing-as-confirmed is possible on this screen.

5. **PRESENT** — demo status is unmistakable: a dashed "DEMO MODE" chip beside the mic, "Start demo recording," "YOUR RECORDINGS (DEMO)," "Start over with the synthetic call script," and "New demo recording" all label the data as synthetic.

6. **ABSENT (N/A)** — no danger/emergency styling is present to compare against caution styling; the strongest accent is the outlined tan "due soon" pill, and nothing alarm-red or filled competes with it.

Additional positive: the consent checkbox starts unchecked and gates the primary button — the UI does not presume consent.

HONESTY-VERDICT: PASS — no violation; the screen is a pre-recording consent view, so the absent banner/badges/provenance chips have nothing to qualify, and all synthetic content is explicitly and repeatedly badged as demo.
