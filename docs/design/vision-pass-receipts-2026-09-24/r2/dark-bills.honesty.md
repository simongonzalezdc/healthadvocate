judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-bills.png)

Image located and viewed — note the declared path was mis-saved: `judge-img/r2` is itself the JPEG (620×436), so I viewed that file (copied to a `.jpg` name only to render it; no other evidence files read, nothing written beyond that temp copy). The screenshot is the dark-mode **Coverage Continuity** screen (nav, heading, "Case title (synthetic)" input, "Create Coverage Case" button, footer disclaimer).

**(1) 'This needs a human decision' banner** — ABSENT. No banner exists anywhere on the screen; there is also no machine answer next to which it could rank, so the "stronger than" test is moot (N/A).

**(2) Neutral 'unavailable' state** — ABSENT. Nothing on the screen uses gray/dashed-border/sentence-case styling; since it doesn't exist, there is nothing that could be mistaken for an urgency rating or danger (N/A).

**(3) Urgency badges (LOW/MEDIUM/HIGH filled pills)** — ABSENT. The only filled pill is the amber "1 reminder due soon" chip in the top bar, which is a reminder notice, not a LOW/MEDIUM/HIGH badge.

**(4) Provenance chips (extracted / inferred / unverified / you-confirmed)** — ABSENT. No provenance chips of any species appear; with none rendered, an inferred value cannot pass as confirmed here (nothing passes as anything).

**(5) Demo/synthetic data clearly badged** — YES. The form is labeled "Case title (synthetic)" and the placeholder reads "e.g. Synthetic job-loss coverage case" — disclosure is present in two places, though the label is small, low-contrast gray text.

**(6) Danger/emergency styling stronger than caution styling** — ABSENT (danger), so unverifiable. The only caution-weight styling is the amber "1 reminder due soon" pill; no red/danger element exists on this screen to compare against, so no violation of the ordering rule is visible, but the rule cannot be affirmatively confirmed from this screenshot alone.

**HONESTY-VERDICT: PASS** — no honesty violation is visible on this screen: it is a benign create-case form with synthetic data disclosed twice, and it contains no fallback, unavailable state, urgency badge, or provenance chip that could masquerade as a confident answer. Caveat: this pass is vacuous for checks 1–4 and 6 (the relevant components simply aren't on this screen); the only positively verifiable check, synthetic-data badging, passes.
