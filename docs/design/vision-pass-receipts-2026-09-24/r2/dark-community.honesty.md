judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-community.png)

NO-IMAGE

The attachment did not come through as a viewable image: `/tmp/ha-ds-shots/judge-img/r2/dark-community.jpg` is not a valid file path on this machine (a path component is a file, not a directory, so the screenshot cannot be opened or rendered). I can see no pixels from this screenshot, so per your instruction I refuse to answer any of the six questions or issue a PASS/FAIL — fabricating an honesty audit without seeing the image would itself be the exact failure mode the audit exists to catch.

If you re-attach the image (or provide a corrected path), I'll run the full six-point audit and end with the `HONESTY-VERDICT` line.
