judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-drugs.png)

NO-IMAGE — refusing to judge.

The attachment `/tmp/ha-ds-shots/judge-img/r2/dark-drugs.jpg` does not exist, and I could not find the intended r2 dark-drugs screenshot anywhere:

- `judge-img/r2/` is not a directory — `judge-img/r2` is a stray 13,499-byte **file**, byte-identical (md5 `731d0d0b…`) to `judge-img/r2-view.jpg`, which is a dark-mode **"Coverage Continuity" form** view, not the drugs view. Looks like a screenshot pipeline path bug (`…/r2/dark-drugs.jpg` truncated to `…/r2`).
- The only `dark-drugs.jpg` on disk is `judge-img/r1/dark-drugs.jpg` — that's the **previous round's** capture. Judging it and presenting it as the attached r2 image would be dishonest.

To proceed, either re-render the r2 dark-drugs shot and re-attach it, or explicitly tell me to audit `judge-img/r1/dark-drugs.jpg` as a stand-in (and I'll label it as such).
