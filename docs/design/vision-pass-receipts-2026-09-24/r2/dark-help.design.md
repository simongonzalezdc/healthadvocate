judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-help.png)

NO-IMAGE — I refuse to fabricate the audit.

The attached path `/tmp/ha-ds-shots/judge-img/r2/dark-help.jpg` does not exist: `judge-img/r2/` is not a directory (`r2` is a 13 KB file), and no `dark-help.jpg` exists anywhere under `r2`. The only file by that name is `/tmp/ha-ds-shots/judge-img/r1/dark-help.jpg` — a **different render round**, so auditing it would silently judge the wrong revision and give you dishonest P0/P1 findings.

Fix the attachment (likely you meant `r2-view.jpg` from this directory, or the r2 export of the help view was never written), resend, and I'll run the full audit against the real pixels.
