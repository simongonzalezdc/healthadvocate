judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-home.png)

NO-IMAGE

I cannot see the attached screenshot. The attachment path `/tmp/ha-ds-shots/judge-img/r2/dark-home.jpg` does not resolve — `judge-img/r2` is a plain 13,499-byte file, not a directory containing `dark-home.jpg`, so the Read tool fails with `ENOTDIR`.

I will not substitute a different file from that directory (e.g., the same-size `r2-attached-dark-insurance.jpg`) and audit it as if it were the attachment — a fallback masquerading as the real thing is exactly what this audit forbids. Per the protocol, no verdict is issued; there is no `HONESTY-VERDICT` line because no image was judged.

If you re-attach the screenshot (or point me at the correct path), I'll run the six-question audit against it.
