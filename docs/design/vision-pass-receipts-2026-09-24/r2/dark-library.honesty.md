judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-library.png)

NO-IMAGE

The attached image cannot be seen, so I refuse to run the audit. Evidence: the attachment reference `/tmp/ha-ds-shots/judge-img/r2/dark-library.jpg` does not resolve — `/tmp/ha-ds-shots/judge-img/r2` is itself a JPEG file (620x436, 13,499 bytes), not a directory, so no `dark-library.jpg` exists under it. No image content was presented to me inline either.

I will not substitute a different file from that directory: the only size-matched candidates are named `r2-attached-dark-insurance.jpg` and `r2-view.jpg`, and the attached name was "dark-library.jpg" — guessing which file is "the" screenshot would risk auditing the wrong image, which is exactly the failure an honesty audit cannot afford. Per the instruction ("If you cannot see the image, say NO-IMAGE and refuse"), there is no HONESTY-VERDICT line. Re-attach the screenshot (or fix the path) and I'll run the six-question audit against it.
