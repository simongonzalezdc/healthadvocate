judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-recorder-live.png)

NO-IMAGE

The attached path does not resolve: `/tmp/ha-ds-shots/judge-img/r2/` is not a directory — `judge-img/r2` is a plain 13.5 KB file — so `r2/dark-recorder-live.jpg` fails (ENOTDIR) and no image was delivered to me.

What actually exists in `/tmp/ha-ds-shots/judge-img/` is `r2` (a file), `r2-view.jpg`, `r2-attached-dark-insurance.jpg`, `r2-form/head/title/top/view-3x.jpg` — nothing named `dark-recorder-live.jpg`. Per instruction I judge only the attached image and will not substitute a guess. Re-send with a valid path (e.g., the intended file re-attached directly) and I'll run the full audit.
