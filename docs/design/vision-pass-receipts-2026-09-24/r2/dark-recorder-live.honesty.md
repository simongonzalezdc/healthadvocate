judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-recorder-live.png)

NO-IMAGE

The attached image could not be seen: the path `/tmp/ha-ds-shots/judge-img/r2/dark-recorder-live.jpg` is not openable — the system reports "Not a directory" (a parent path component is not a directory), so no image content was ever rendered to me. I will not search the filesystem for a substitute image, since the audit mandates judging only the attached one.

Per the audit rules, I refuse to answer the six numbered questions or issue an HONESTY-VERDICT, because any PASS/FAIL without seeing the pixels would itself violate the honesty standard being audited. Please re-attach the screenshot (verify the file exists and the path is correct) and I will run the full audit.
