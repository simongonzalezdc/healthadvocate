judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-symptoms-emergency.png)

NO-IMAGE — I cannot see the attached screenshot and must refuse the audit.

The attachment path is invalid: `/tmp/ha-ds-shots/judge-img/r2` is a 13,499-byte **file**, not a directory (hence the `ENOTDIR` error), and no `dark-symptoms-emergency.jpg` exists anywhere under `/tmp/ha-ds-shots/`. The only similarly sized file, `r2-attached-dark-insurance.jpg`, appears to be a *different* screenshot (dark-insurance, per its name), so substituting it would violate "judge ONLY the attached image" and produce a dishonest audit.

Fix the harness path (likely intended `/tmp/ha-ds-shots/judge-img/r2/dark-symptoms-emergency.jpg` where `r2` should be a directory, or the filename got clobbered) and re-attach; I'll judge it then.
