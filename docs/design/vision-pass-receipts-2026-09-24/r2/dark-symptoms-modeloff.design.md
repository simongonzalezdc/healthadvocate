judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-symptoms-modeloff.png)

NO-IMAGE — refusing the audit.

The attached path does not resolve: `/tmp/ha-ds-shots/judge-img/r2/` is not a directory (Read fails with ENOTDIR; `r2` is a 13.5 KB file, not a folder), so `dark-symptoms-modeloff.jpg` does not exist at the stated location. The directory does contain same-size files named differently (`r2`, `r2-view.jpg`, `r2-attached-dark-insurance.jpg` — all 13,499 bytes, suggesting a dark *insurance* view, not symptoms), plus light-mode symptoms shots elsewhere — but per the brief I judge only the attached image, and substituting a differently-named screenshot would risk auditing the wrong view. Re-attach the correct file and I'll run the full audit.
