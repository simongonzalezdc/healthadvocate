judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/dark-symptoms.png)

NO-IMAGE

I cannot see the attached screenshot. The only legitimate way for me to view it (reading the attachment at `/tmp/ha-ds-shots/judge-img/r2/dark-symptoms.jpg`) failed with a filesystem error — `ENOTDIR: not a directory`, meaning that path does not resolve to a viewable image file. Per your instruction, and per honesty policy (I will not fabricate an audit of an image I cannot see), I refuse to answer the six numbered questions, and I issue no verdict line.

Suggested fix on your side: verify the path exists and is a file (the `r2` component of the path may itself be a file, not a directory), re-render or re-copy the screenshot, and re-attach it — I'll run the full audit the moment I can actually see it.
