judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-community.png)

NO-IMAGE — refusing the audit.

The attached path `/tmp/ha-ds-shots/judge-img/r2/light-community.jpg` does not resolve: `judge-img/r2` is a 13,499-byte file, not a directory, so no `light-community.jpg` exists behind it. Nothing named `light-community.jpg` exists in `judge-img/` either (only `r2*` crops, `z-*` crops, and an `r1/` folder — none bearing that name).

I won't substitute a different image and audit it as if it were the attached one — that would be a dishonest review. Re-export or re-attach the correct file and I'll run the full describe → P0/P1/P2 pass.
