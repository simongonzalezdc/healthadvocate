judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-community.png)

NO-IMAGE — I cannot see the attached image, so I'm refusing the audit per your instruction.

What happened: the stated attachment path `/tmp/ha-ds-shots/judge-img/r2/light-community.jpg` does not exist. `/tmp/ha-ds-shots/judge-img/r2` is itself a 13,499-byte file (not a directory), so no `light-community.jpg` can exist beneath it, and no image content was rendered into my context. I will not guess or substitute a nearby file (e.g., `r2-view.jpg` or the `r2` file itself) for the named screenshot — an honesty audit of the wrong pixels is worse than no audit.

If you re-attach the intended image (or give a path that resolves), I'll run the six numbered checks and end with the `HONESTY-VERDICT` line.
