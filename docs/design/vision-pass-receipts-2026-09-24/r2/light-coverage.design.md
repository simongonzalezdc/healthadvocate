judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-coverage.png)

NO-IMAGE — refusing.

The attached path `/tmp/ha-ds-shots/judge-img/r2/light-coverage.jpg` does not exist: under `/tmp/ha-ds-shots/judge-img/`, `r2` is a 13,499-byte file, not a directory, so nothing named `light-coverage.jpg` can resolve. No image was rendered for me to judge.

I will not substitute a look-alike from that folder (e.g., `r2-view.jpg` or `r2-attached-dark-insurance.jpg`, the latter byte-identical in size to `r2`), because the filename promises a "light-coverage" view and guessing would risk auditing the wrong screen — a dishonest audit.

Fix the harness path (or re-attach the intended screenshot) and I'll run the full audit — description, then P0/P1/P2 defects with locations — on the actual image.
