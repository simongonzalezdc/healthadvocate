judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-directory-live.png)

NO-IMAGE — refusing the audit.

The attached path `/tmp/ha-ds-shots/judge-img/r2/light-directory-live.jpg` does not exist: its parent `r2` is a plain 13.5 KB file, not a directory, so the path can't resolve and no image was rendered to me. I will not substitute a lookalike sibling (`r2-view.jpg`, `r2-attached-dark-insurance.jpg` are the same byte size and likely the actual capture — and their naming suggests a dark insurance view, contradicting the "light-directory-live" label). Auditing the wrong frame would be a dishonest verdict. Re-attach with a valid path, or point me at the correct file, and I'll run the full audit.
