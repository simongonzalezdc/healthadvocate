judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-documents.png)

NO-IMAGE

I cannot see the attached image, and I'm refusing to fabricate an audit. Details:

- The stated attachment path `/tmp/ha-ds-shots/judge-img/r2/light-documents.jpg` is invalid: `judge-img/r2` is itself a JPEG file (620×436, 13,499 bytes), not a directory, so the path fails with `ENOTDIR` and cannot be read as given.
- No file named `light-documents*` exists anywhere under `/tmp`. The similarly named files that do exist (`r2`, `r2-attached-dark-insurance.jpg`, `r2-view.jpg` — apparently byte-identical at 13,499 bytes) appear to be a different capture, likely the dark-theme insurance screen, not the light documents screenshot this audit is scoped to.

Since this is an honesty audit where judging the wrong screenshot would itself be a false result, I will not substitute a different image or guess. Re-attach `light-documents.jpg` (or place it at a valid path) and I'll run the six-question audit against exactly that image.
