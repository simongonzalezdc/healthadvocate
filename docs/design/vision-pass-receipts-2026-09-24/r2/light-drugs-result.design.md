judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-drugs-result.png)

NO-IMAGE — refusing to audit.

The attached path `/tmp/ha-ds-shots/judge-img/r2/light-drugs-result.jpg` does not resolve: under `/tmp/ha-ds-shots/judge-img/`, `r2` is a plain 13.5 KB file, not a directory, so no `light-drugs-result.jpg` exists inside it. Per your instruction I will not substitute or guess at a different file (note: the stray `r2` file is byte-for-byte the same size as `r2-attached-dark-insurance.jpg`, so it is plausibly the *dark insurance* shot — auditing it as "light-drugs-result" would be dishonest).

Existing sibling candidates, if you meant one of those: `r2-view.jpg`, `r2-view-3x.jpg`, `r2-form.jpg`, `r2-head.jpg`, `r2-top.jpg`, `r2-title.jpg`. Point me at the correct image and I'll run the full audit.
