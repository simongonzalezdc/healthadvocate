judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-drugs-result.png)

NO-IMAGE — refusing the audit.

The attached image cannot be seen because the path is broken: `/tmp/ha-ds-shots/judge-img/r2` is a JPEG file (620x436), not a directory, so `/tmp/ha-ds-shots/judge-img/r2/light-drugs-result.jpg` cannot exist. There is no `r2/` subdirectory in `judge-img/`; the only round folder is `r1/`, which does contain a `light-drugs-result.jpg`, but per your rule ("Judge ONLY the attached image") I will not substitute it.

If you meant the round-1 screenshot, re-run the audit with the path `/tmp/ha-ds-shots/judge-img/r1/light-drugs-result.jpg`, or re-attach the intended image.
