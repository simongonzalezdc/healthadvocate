judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-family.png)

NO-IMAGE — refusing to judge.

The attached path `/tmp/ha-ds-shots/judge-img/r2/light-family.jpg` does not exist: in `/tmp/ha-ds-shots/judge-img/`, `r2` is itself a 620×436 JPEG file, not a directory, so it cannot contain `light-family.jpg`. A same-named file exists at `/tmp/ha-ds-shots/judge-img/r1/light-family.jpg`, but that is the r1 capture, not the attached r2 one — auditing it would produce a verdict about the wrong round, and I was instructed to judge only the attached image.

Fix: re-attach with the correct path (either the intended `r1/light-family.jpg`, or rename/copy the r2 capture if `r2`'s 13.5 KB file is the intended subject — note it is unusually small and likely a duplicate of `r2-attached-dark-insurance.jpg`, which may itself be the real defect).
