judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-help.png)

NO-IMAGE — refusing the audit.

The attached path `/tmp/ha-ds-shots/judge-img/r2/light-help.jpg` does not exist: `judge-img/r2` is a plain 13.5 KB file (no extension), not a directory, so there is no `light-help.jpg` inside it to view. I did not substitute any other file, since several same-round candidates exist (`r2-view.jpg`, `r2-form.jpg`, `r2-head.jpg`, etc.) and I can't know which was intended — the instruction is to judge only the attached image.

To get the review, re-attach the correct path (e.g., if `r2` is a mis-named copy of the screenshot, re-save it as `r2/light-help.jpg` or point me at the exact `.jpg`), and I'll run the full describe → P0/P1/P2 audit against the 'warm paper clinic' system.
