judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-help.png)

NO-IMAGE — I cannot audit this screenshot and refuse to issue a verdict.

The attachment path `/tmp/ha-ds-shots/judge-img/r2/light-help.jpg` does not resolve to a file: `judge-img/r2` exists as a plain file (13,499 bytes), not a directory, so the path beneath it fails with `ENOTDIR`. No `light-help.jpg` exists anywhere under `judge-img/`.

There are similarly named images present (`r2` — byte-identical in size to `r2-attached-dark-insurance.jpg` and `r2-view.jpg` — plus `r2-form.jpg`, `r2-head.jpg`, `r2-top.jpg`, etc.), but since the task is a strict single-image honesty audit and I can't confirm which image was actually attached, guessing would risk judging the wrong screenshot and emitting a false PASS/FAIL. Per your instruction, I stop here: NO-IMAGE. If you re-attach the intended JPEG (or fix the path), I'll run the six-question audit against exactly that image.
