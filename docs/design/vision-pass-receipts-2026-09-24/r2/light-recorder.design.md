judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-recorder.png)

NO-IMAGE

The attached image cannot be seen. `/tmp/ha-ds-shots/judge-img/r2/light-recorder.jpg` does not resolve — in that path, `r2` is a 13 KB regular file, not a directory, so the path is invalid and no image was delivered to me.

I will not substitute a guess: a similarly named capture (`/tmp/ha-ds-shots/t-light-recorder-summary.jpg`) exists on disk, but it is a different file, and auditing it as if it were the attachment would be dishonest — exactly the kind of defect ("dishonest") this rubric is meant to catch. Re-attach the screenshot (or fix the path) and I'll run the full audit: description, then P0/P1/P2 defects with locations.
