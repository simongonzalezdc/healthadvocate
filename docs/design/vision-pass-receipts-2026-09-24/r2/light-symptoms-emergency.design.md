judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/light-symptoms-emergency.png)

NO-IMAGE

The attached image cannot be seen, and I'm refusing to substitute a different one. Evidence:

- `/tmp/ha-ds-shots/judge-img/r2/light-symptoms-emergency.jpg` does not exist — `judge-img/r2` is a file (13,499 bytes, JPEG), not a directory, so the path fails with ENOTDIR.
- Those `r2` bytes are byte-identical (md5 `731d0d0b…`) to `r2-attached-dark-insurance.jpg` and `r2-view.jpg` — i.e., the file actually sitting at that path is a 620×436 shot named as the **dark insurance** view, not the light symptoms-emergency view you asked me to judge.
- A `light-symptoms-emergency.jpg` exists only under `judge-img/r1/` — a previous round's capture, which I won't quietly audit and mislabel as the attached r2 image.

Judging any of those would mean reviewing the wrong screen (or a stale round) and reporting it as this one — exactly the "dishonest" defect category. Re-attach the correct r2 `light-symptoms-emergency.jpg` (or fix the path so the named file actually exists) and I'll run the full audit: description, then P0/P1/P2 defects with locations.
