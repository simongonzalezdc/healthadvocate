judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/mobile-directory.png)

NO-IMAGE — I cannot see the attached screenshot, so I'm refusing to issue a verdict rather than auditing a substitute.

**Why:** The attachment path `/tmp/ha-ds-shots/judge-img/r2/mobile-directory.jpg` does not exist. In `/tmp/ha-ds-shots/judge-img/`, `r2` is a 13 KB JPEG **file**, not a directory, so no screenshot can live under it. A file named `mobile-directory.jpg` does exist at `/tmp/ha-ds-shots/judge-img/r1/mobile-directory.jpg` (round r1), but the r2 set is a different capture batch (it contains insurance-related shots like `r2-attached-dark-insurance.jpg`), so substituting the r1 file would risk auditing the wrong screen — exactly the kind of confident answer your honesty rule forbids.

**No numbered questions answered, no verdict issued.** If you meant the r1 screenshot, re-attach `/tmp/ha-ds-shots/judge-img/r1/mobile-directory.jpg` (or confirm it's the intended image) and I'll run the full six-question audit against it.
