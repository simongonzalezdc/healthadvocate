judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/mobile-home.png)

NO-IMAGE — refusing to audit.

The attached path `/tmp/ha-ds-shots/judge-img/r2/mobile-home.jpg` does not resolve: in `/tmp/ha-ds-shots/judge-img/`, `r2` is a plain 13 KB file, not a directory, so no such screenshot exists to view. I won't substitute a different frame (e.g., `judge-img/r1/mobile-home.jpg` or `judge-img/r2-attached-dark-insurance.jpg`) and judge it as if it were the attached one — that would be a dishonest audit.

If you meant one of those files, re-attach it explicitly and I'll run the full describe-then-defects pass.
