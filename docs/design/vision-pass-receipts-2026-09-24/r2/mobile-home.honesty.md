judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/mobile-home.png)

NO-IMAGE

I cannot see the attached screenshot, so I'm refusing to render a verdict, per your protocol. Evidence from the diagnostic check only:

- The attached path `/tmp/ha-ds-shots/judge-img/r2/mobile-home.jpg` does not exist — in `/tmp/ha-ds-shots/judge-img/`, `r2` is a plain 13,499-byte file, not a directory, so nothing can live under `r2/mobile-home.jpg`.
- Several other images sit nearby (`r2-view.jpg`, `r2-attached-dark-insurance.jpg` — both the same 13,499-byte size — plus `r2-form.jpg`, `r2-head.jpg`, etc.), but none is named `mobile-home.jpg`, and I have no way to know which pixels were actually attached. Substituting a lookalike file and auditing it as "the attached image" would be exactly the kind of fabricated confidence this audit exists to catch, so I won't do it.

No numbered questions were evaluated; no verdict is issued. Re-attach the correct image (a real file path ending in `mobile-home.jpg`) and I'll run the six-question audit against it.
