judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r2/mobile-recorder.png)

NO-IMAGE

The attachment path is broken: `/tmp/ha-ds-shots/judge-img/r2/mobile-recorder.jpg` does not exist. `judge-img/r2` is itself a single JPEG file (620×436), not a directory, so no `mobile-recorder.jpg` inside it can be resolved. I could not view the intended image.

Per the audit's own terms — judge only the attached image, and refuse if it cannot be seen — I will not substitute a different file from that directory, since auditing the wrong screenshot would be worse than auditing none.

If the intended attachment was the JPEG at `/tmp/ha-ds-shots/judge-img/r2` (no `mobile-recorder.jpg` component), re-attach with the correct path and I'll run the full six-question audit and issue the HONESTY-VERDICT line.
