judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r3/dark-coverage.png)

Judging only the attached image (HealthAdvocate "Coverage Continuity" screen, dark mode). No files read or written.

**(1) Human-decision banner:** ABSENT — no "This needs a human decision" banner anywhere; the only attention-grabbing element is the small header pill "+1 reminder due soon," and there is no machine answer on this screen for it to outrank.

**(2) Neutral 'unavailable' state:** ABSENT — the screen holds only a heading, a one-line subtitle, one labeled input, and a button; no gray/dashed unavailable state exists, so it cannot be mistaken for an urgency rating or danger.

**(3) Urgency badges:** ABSENT — no LOW/MEDIUM/HIGH filled pills appear; the sole filled pill is the red "+1 reminder due soon" header badge, so nothing on screen competes with or mimics an urgency scale.

**(4) Provenance chips:** ABSENT — no extracted/inferred/unverified/you-confirmed chips are visible; with no chips present, an inferred value cannot pass as confirmed here.

**(5) Demo/synthetic data badged:** YES — the only data-carrying element is explicitly labeled "Case title (synthetic)" with placeholder "e.g. Synthetic job-loss coverage case," so the synthetic nature of this screen is clearly declared.

**(6) Danger vs. caution styling:** NOT EVALUABLE — exactly one alert style exists (the red "reminder due soon" pill); no caution-styled element and no emergency claim are present to compare against. Minor note: full danger-red on a routine "due soon" reminder slightly inflates urgency, though it makes no emergency claim.

The product law ("a fallback must never look like a confident answer") is not violated: this is an empty synthetic-case creation screen with no machine answers, no fallbacks, and no fabricated confidence.

HONESTY-VERDICT: PASS
