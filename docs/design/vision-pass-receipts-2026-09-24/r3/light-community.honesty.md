judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r3/light-community.png)

I can see the image directly — no file reads needed. It shows the HealthAdvocate "Community Health Scanner" empty input screen (paste box, Scan button, nav bar, footer disclaimer), with no scan results displayed.

1. **'This needs a human decision' banner — ABSENT.** No banner exists, but there is also no machine answer on screen (only an empty paste form), so nothing is competing for trust; the footer does carry "support, not replace, professional medical guidance."

2. **Neutral 'unavailable' state — ABSENT.** No gray/dashed unavailable element is shown; the only gray text is the textarea placeholder ("Paste a news article, CDC alert, FDA recall…"), which reads as placeholder, not as a status, urgency rating, or danger.

3. **Urgency badges — ABSENT.** No LOW/MEDIUM/HIGH filled pills appear; the only pill is the amber-outlined "1 reminder due soon" in the header, which is a calendar reminder, not an urgency rating.

4. **Provenance chips — ABSENT.** No extracted/inferred/unverified/you-confirmed chips are visible, so no inferred value can pass as confirmed on this screen.

5. **Demo/synthetic data badge — ABSENT.** No synthetic-data badge is shown; nothing on screen is presented as data at all (empty input state), so nothing is mislabeled.

6. **Danger vs. caution styling — NOT EVALUABLE (both absent).** There is no danger/emergency styling anywhere; the strongest attention element is the amber "1 reminder due soon" outline pill, which is mild and correctly subdued for its meaning.

**HONESTY-VERDICT: PASS** — no violations on this screen: it is the pre-scan empty state with no machine answers, fallbacks, urgency badges, or provenance chips displayed, so nothing masquerades as a confident answer. Note: this screenshot cannot verify the audited states (banner, unavailable style, badges, chips, danger styling) because none are rendered here — those gates need a results-view or fallback-state screenshot to be meaningfully judged.
