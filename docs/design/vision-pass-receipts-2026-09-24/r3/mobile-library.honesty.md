judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r3/mobile-library.png)

Judging the attached image only (mobile Library screen).

1. **'This needs a human decision' banner: ABSENT.** No such banner anywhere on the screen; the only machine-derived item (the "Appeal window closes (detected)" card) carries a "(detected)" qualifier and a source chip, so nothing near it is competing as an unflagged confident answer.

2. **Neutral 'unavailable' state: ABSENT.** No gray/dashed sentence-case "unavailable" chip exists; the only dashed-border element is the DEMO DATA badge, and since nothing on this list is shown as unavailable, there is nothing that could be mistaken for an urgency or danger signal.

3. **Urgency badges: ABSENT.** No LOW/MEDIUM/HIGH filled pills — all chips are soft-tinted category tags ("denial — not medically necessary", "physical therapy", "screening results"), so distinctness from an unavailable state cannot be assessed because neither is exercised.

4. **Provenance chips: PARTIAL — only one species present.** A single extracted-style chip "• FROM THE DENIAL LETTER" appears (Appeal card); extracted/inferred/unverified/you-confirmed are not all shown, so cross-species distinguishability can't be verified from this image — but no inferred value is seen passing as confirmed, since the deadline is explicitly titled "(detected)" and dated "Oct 8" in a muted gray rather than asserted styling.

5. **Demo/synthetic data badged: YES.** A dashed-border, all-caps "DEMO DATA" badge sits in the Library header card, visible without scrolling; minor cosmetic note: a stray "0" abuts the badge's top-left corner, which reads as a rendering artifact rather than a label.

6. **Danger stronger than caution: YES.** The only danger element ("Delete") uses red outline + red text, clearly the strongest signal on screen, while caution-flavored items ("appeal window", "30-day appeal window", "$1,200.00", "Sep 30") are pale orange tints — an unambiguous hierarchy; no emergency styling exists on this screen to compare against.

Notes (not violations): the absent banner, unavailable state, urgency pills, and three provenance species are simply not exercised on a library/index screen, which presents no fallback masquerading as an answer; footer correctly disclaims "support, not replace… always consult a qualified healthcare provider."

HONESTY-VERDICT: PASS — no violation of the fallback law observed; flag for follow-up: the provenance chip system shows only one species here, so its species-distinguishability should be verified on a screen that renders extracted/inferred/unverified side by side.
