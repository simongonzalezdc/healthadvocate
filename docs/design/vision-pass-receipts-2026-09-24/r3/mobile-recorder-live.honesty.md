judged-by: GLM-5.3-Flash (zai/glm-5.3-flash, flash-home zcode lane; full-res original: screenshots/r3/mobile-recorder-live.png)

I can see the attached image (HealthAdvocate, Recorder tab, live demo recording). Audit below, judging only this screenshot — no files read or written.

1. **'This needs a human decision' banner: ABSENT** — no such banner anywhere; this screen shows only a transcript and controls, with no machine answer/summary near which a banner would need to outrank.

2. **Neutral 'unavailable' state: ABSENT** — no gray/dashed/sentence-case unavailable element exists; the only dashed element is the navy "DEMO MODE" chip, which is uppercase and reads as a mode label, not something mistakable for an urgency rating or danger.

3. **Urgency badges: ABSENT** — no LOW/MEDIUM/HIGH filled pills; the only filled red elements are the recording dot, waveform bars, and "Stop & save" button, which are clearly record controls.

4. **Provenance chips: ABSENT** — no extracted/inferred/unverified/you-confirmed chips appear; the only labels are speaker tags "YOU" and "INSURER REP," so an inferred value cannot pass as confirmed because no derived values are shown. The legend below ("Italic text is still a draft… *[inaudible]* … the recorder never guesses") is honest fallback language.

5. **Demo/synthetic data badged: PRESENT** — badged twice: a dashed "DEMO MODE" chip beside the feature description and "RECORDING · DEMO" beside the 00:18 timer, so the demo state is hard to miss.

6. **Danger vs. caution styling: NO — neither is present** — there is no emergency or caution banner to compare; the only red is the functional recording indicator/button and the amber "1" is a header count badge, so no hierarchy is violated on this screen.

No confident-looking fallback appears anywhere: the transcript's uncertainty behavior is explicitly disclosed ("the recorder never guesses"), and the footer carries the "support, not replace" disclaimer. The absent elements (banner, urgency badges, provenance chips) are not applicable to this recorder screen since no analysis output is displayed.

**HONESTY-VERDICT: PASS** — no violation found; demo status is clearly badged and all machine-uncertainty language is honest.
