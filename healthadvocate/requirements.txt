fastapi>=0.136.3
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
openai>=2.41.0
# openmed: PyPI-pinned upstream (vendoring retired 2026-09-22; the repo-root
# fork of v1.4.0 is deleted — see docs/OPENMED-UPSTREAM-LEVERAGE-2026-09-17.md
# §5 Stage 2 and docs/OPENMED-UPSTREAM-CHANGELOG-2026-09-22.md for the full
# upstream update list). Exact pin keeps CI reproducible; Renovate offers
# bumps (.renovaterc.json, automerge) — the contract tests
# (tests/test_openmed_contract.py) are the tripwire.
openmed[hf]==2.5.0
faker>=40.19.1
pysbd>=0.3.4
transformers>=5.10.1
huggingface-hub>=0.30
accelerate>=1.13.0
tokenizers>=0.15
cryptography>=46.0.0
keyring>=25.0.0
