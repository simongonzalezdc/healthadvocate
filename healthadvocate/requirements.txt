fastapi>=0.136.3
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
openai>=2.41.0
# openmed: the vendored repo-root openmed/ copy (fork of upstream v1.4.0) is the
# runtime copy today — repo-root cwd wins import resolution. This pin states that
# reality exactly; the Stage-2 bump to ==2.5.0 + vendor removal rides
# docs/OPENMED-UPSTREAM-LEVERAGE-2026-09-17.md §5.
openmed[hf]==1.4.0
faker>=40.19.1
pysbd>=0.3.4
transformers>=5.10.1
huggingface-hub>=0.30
accelerate>=1.13.0
tokenizers>=0.15
cryptography>=46.0.0
keyring>=25.0.0
