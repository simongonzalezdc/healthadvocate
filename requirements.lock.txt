# HealthAdvocate transitive dependency lock — generated 2026-09-23 from a
# clean-room venv built from healthadvocate/requirements.txt (openmed[hf]==2.5.0
# era). Purpose: build reproducibility — revert-and-rebuild reproduces the
# validated environment (RALPLAN Wave 3, Architect amendment 2; the plan's
# acceptance: fresh venv from this file + import probe openmed 2.5.0 from
# site-packages + full suite green in that venv).
# Rebuild: python3.13 -m venv /tmp/v && /tmp/v/bin/pip install -r requirements.lock.txt pytest httpx
# Regenerate: clean venv -> pip install -r healthadvocate/requirements.txt -> pip freeze (exclude pip)
